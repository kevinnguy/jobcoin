import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  textInput: {
    height: 50,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: 'gray',
    paddingLeft: 10,
    fontSize: 16,
  },
});
